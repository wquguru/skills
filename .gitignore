*.swp

# Vendored third-party skills — rehydrated from external.yml via `just vendor`.
# Only external.yml + external.lock are tracked; the upstream code is not, so this
# repo stays purely self-written.
/external/
